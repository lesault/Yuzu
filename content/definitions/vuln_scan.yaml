## Host Vulnerability Scanning Definitions
## Matches installed software against known CVEs and runs configuration
## compliance checks. Reports findings by severity.
## Plugin: vuln_scan | Actions: scan, cve_scan, config_scan, summary, inventory,
##                               update_rules, kernel_scan, binary_scan

---
apiVersion: yuzu.io/v1alpha1
kind: InstructionDefinition
metadata:
  id: security.vuln_scan.scan
  displayName: Full Vulnerability Scan
  version: 1.0.0
  description: >
    Run a full host vulnerability scan combining CVE matching and
    configuration compliance checks. CVE matching compares installed
    software versions against a built-in rule database. Configuration
    checks verify security hardening settings (firewall state, password
    policy, remote desktop config, etc.). Each finding is classified by
    severity (CRITICAL, HIGH, MEDIUM, LOW, INFO). Reports progress at
    0%, 50%, 90%, 100%.
  tags: [security, vulnerability, cve, compliance, audit]

spec:
  type: question
  platforms: [windows, linux, darwin]

  execution:
    plugin: vuln_scan
    action: scan
    concurrency: per-device

  parameters:
    type: object
    properties: {}

  result:
    columns:
      - name: severity
        type: string
      - name: category
        type: string
      - name: title
        type: string
      - name: detail
        type: string
    aggregation:
      groupBy: [severity, category]
      operations: [count]

  visualization:
    # vuln_scan emits one row per finding on `scan`, format
    # `severity|category|title|detail`. labelField=0 picks severity so
    # CRITICAL / HIGH / MEDIUM / LOW / INFO each become a pie slice and
    # findings are counted into them. Preferred over the summary action's
    # pie because summary also emits a TOTAL row that would dominate the
    # chart.
    type: pie
    processor: single_series
    title: Vulnerabilities by severity
    labelField: 0
    valueLabel: Findings

  readablePayload: "Run full vulnerability scan (CVE + configuration)"

  gather:
    ttlSeconds: 600

  approval:
    mode: auto

  permissions:
    executeRoles: [endpoint-admin, endpoint-operator, security-admin]
    authorRoles: [content-author]

  compatibility:
    minAgentVersion: 1.0.0
    requiredPlugins: [vuln_scan]

status:
  phase: active

---
apiVersion: yuzu.io/v1alpha1
kind: InstructionDefinition
metadata:
  id: security.vuln_scan.cve_scan
  displayName: CVE Vulnerability Scan
  version: 1.0.0
  description: >
    Scan installed software against known CVE rules. Enumerates installed
    applications (registry on Windows, dpkg/rpm/pacman on Linux,
    system_profiler/brew on macOS) and compares versions against the
    built-in CVE database. Returns only CVE findings, not configuration
    checks. Use the full "scan" action for combined results.
  tags: [security, vulnerability, cve, compliance]

spec:
  type: question
  platforms: [windows, linux, darwin]

  execution:
    plugin: vuln_scan
    action: cve_scan
    concurrency: per-device

  parameters:
    type: object
    properties: {}

  result:
    columns:
      - name: severity
        type: string
      - name: category
        type: string
      - name: title
        type: string
      - name: detail
        type: string
    aggregation:
      groupBy: [severity]
      operations: [count]

  readablePayload: "Run CVE vulnerability scan"

  gather:
    ttlSeconds: 600

  approval:
    mode: auto

  permissions:
    executeRoles: [endpoint-admin, endpoint-operator, security-admin]
    authorRoles: [content-author]

  compatibility:
    minAgentVersion: 1.0.0
    requiredPlugins: [vuln_scan]

status:
  phase: active

---
apiVersion: yuzu.io/v1alpha1
kind: InstructionDefinition
metadata:
  id: security.vuln_scan.config_scan
  displayName: Configuration Compliance Scan
  version: 1.0.0
  description: >
    Run security configuration compliance checks on the endpoint.
    Verifies settings such as firewall state, password policy,
    remote desktop configuration, and other hardening baselines.
    Returns findings classified by severity. Does not include CVE
    matching; use the full "scan" action for combined results.
  tags: [security, compliance, configuration, hardening]

spec:
  type: question
  platforms: [windows, linux, darwin]

  execution:
    plugin: vuln_scan
    action: config_scan
    concurrency: per-device

  parameters:
    type: object
    properties: {}

  result:
    columns:
      - name: severity
        type: string
      - name: category
        type: string
      - name: title
        type: string
      - name: detail
        type: string
    aggregation:
      groupBy: [severity]
      operations: [count]

  readablePayload: "Run configuration compliance scan"

  gather:
    ttlSeconds: 300

  approval:
    mode: auto

  permissions:
    executeRoles: [endpoint-admin, endpoint-operator, security-admin]
    authorRoles: [content-author]

  compatibility:
    minAgentVersion: 1.0.0
    requiredPlugins: [vuln_scan]

status:
  phase: active

---
apiVersion: yuzu.io/v1alpha1
kind: InstructionDefinition
metadata:
  id: security.vuln_scan.summary
  displayName: Vulnerability Scan Summary
  version: 1.0.0
  description: >
    Run a full vulnerability scan (CVE + configuration) and return
    only the severity summary counts. Provides a quick overview of
    the endpoint's security posture without the full finding details.
    Returns total findings, total issues, and per-severity counts
    (CRITICAL, HIGH, MEDIUM, LOW, INFO).
  tags: [security, vulnerability, compliance, dashboard]

spec:
  type: question
  platforms: [windows, linux, darwin]

  execution:
    plugin: vuln_scan
    action: summary
    concurrency: per-device

  parameters:
    type: object
    properties: {}

  result:
    columns:
      - name: severity
        type: string
      - name: count
        type: int32

  readablePayload: "Get vulnerability scan summary"

  gather:
    ttlSeconds: 600

  approval:
    mode: auto

  permissions:
    executeRoles: [endpoint-admin, endpoint-operator, security-admin]
    authorRoles: [content-author]

  compatibility:
    minAgentVersion: 1.0.0
    requiredPlugins: [vuln_scan]

status:
  phase: active

---
apiVersion: yuzu.io/v1alpha1
kind: InstructionDefinition
metadata:
  id: security.vuln_scan.inventory
  displayName: Software Inventory for NVD Matching
  version: 1.0.0
  description: >
    Return the raw installed software inventory (name and version pairs)
    for server-side NVD matching. This is a lightweight alternative to
    client-side CVE scanning that enables the server to perform
    centralized vulnerability correlation against the full NVD database.
  tags: [security, vulnerability, inventory, software]

spec:
  type: question
  platforms: [windows, linux, darwin]

  execution:
    plugin: vuln_scan
    action: inventory
    concurrency: per-device

  parameters:
    type: object
    properties: {}

  result:
    columns:
      - name: name
        type: string
      - name: version
        type: string

  readablePayload: "Collect software inventory for NVD matching"

  gather:
    ttlSeconds: 300

  approval:
    mode: auto

  permissions:
    executeRoles: [endpoint-admin, endpoint-operator, security-admin]
    authorRoles: [content-author]

  compatibility:
    minAgentVersion: 1.0.0
    requiredPlugins: [vuln_scan]

status:
  phase: active

---
apiVersion: yuzu.io/v1alpha1
kind: InstructionDefinition
metadata:
  id: security.vuln_scan.update_rules
  displayName: Update CVE Rules
  version: 1.0.0
  description: >
    Reload the CVE ruleset from a previously staged JSON file at
    <data_dir>/staged/cve_rules.json. Use after running content_dist.stage
    to push an updated cve_rules.json to the endpoint. The compiled-in
    critical-CVE fallback remains active regardless of load success.
  tags: [security, vulnerability, maintenance]

spec:
  type: action
  platforms: [windows, linux, darwin]

  execution:
    plugin: vuln_scan
    action: update_rules
    concurrency: per-device
    timeoutSeconds: 30

  result:
    columns:
      - name: status
        type: string
      - name: category
        type: string
      - name: title
        type: string
      - name: detail
        type: string

  readablePayload: "Reload CVE rules from staged file"

  approval:
    mode: auto

  permissions:
    executeRoles: [endpoint-admin, security-admin]
    authorRoles: [content-author]

  compatibility:
    minAgentVersion: 1.0.0
    requiredPlugins: [vuln_scan]

status:
  phase: active

---
apiVersion: yuzu.io/v1alpha1
kind: InstructionDefinition
metadata:
  id: security.vuln_scan.kernel_scan
  displayName: Kernel CVE Scan
  version: 1.0.0
  description: >
    Detects CVEs in the running OS kernel version. Uses uname() on Linux,
    registry CurrentBuildNumber/UBR on Windows, and sw_vers on macOS.
    Matches against kernel-specific rules in the active ruleset
    (product token "linux-kernel", "windows-kernel", or "macos").
  tags: [security, vulnerability, kernel]

spec:
  type: question
  platforms: [windows, linux, darwin]

  execution:
    plugin: vuln_scan
    action: kernel_scan
    concurrency: per-device
    timeoutSeconds: 30

  result:
    columns:
      - name: severity
        type: string
      - name: category
        type: string
      - name: title
        type: string
      - name: detail
        type: string
    aggregation:
      groupBy: [severity]
      operations: [count]

  readablePayload: "Scan kernel version for CVEs"

  gather:
    ttlSeconds: 600

  approval:
    mode: auto

  permissions:
    executeRoles: [endpoint-admin, endpoint-operator, security-admin]
    authorRoles: [content-author]

  compatibility:
    minAgentVersion: 1.0.0
    requiredPlugins: [vuln_scan]

status:
  phase: active

---
apiVersion: yuzu.io/v1alpha1
kind: InstructionDefinition
metadata:
  id: security.vuln_scan.binary_scan
  displayName: Binary File Version Scan
  version: 1.0.0
  description: >
    Reads version information directly from installed binary files — PE
    VERSIONINFO resources on Windows, Info.plist on macOS, epoch-stripped
    dpkg/rpm versions on Linux — and compares against CVE rules. Detects
    drift when an admin manually installs a version that differs from what
    the package manager recorded.
  tags: [security, vulnerability, binary]

spec:
  type: question
  platforms: [windows, linux, darwin]

  execution:
    plugin: vuln_scan
    action: binary_scan
    concurrency: per-device
    timeoutSeconds: 120

  parameters:
    type: object
    properties:
      paths:
        type: string
        displayName: Binary paths
        description: >
          Comma-separated list of binary paths to inspect. Leave empty to
          use the default high-value binary list for the current platform.
        default: ""

  result:
    columns:
      - name: severity
        type: string
      - name: category
        type: string
      - name: title
        type: string
      - name: detail
        type: string
    aggregation:
      groupBy: [severity]
      operations: [count]

  readablePayload: "Scan binary file versions for CVEs"

  gather:
    ttlSeconds: 600

  approval:
    mode: auto

  permissions:
    executeRoles: [endpoint-admin, endpoint-operator, security-admin]
    authorRoles: [content-author]

  compatibility:
    minAgentVersion: 1.0.0
    requiredPlugins: [vuln_scan]

status:
  phase: active

---
apiVersion: yuzu.io/v1alpha1
kind: InstructionDefinition
metadata:
  id: security.vulnerability.pkg_scan
  displayName: Language Package CVE Scan (Offline)
  version: 1.0.0
  description: >
    Detect globally installed language-specific packages (npm, pip, cargo, gem,
    NuGet) and match them offline against ecosystem-tagged CVE rules. This action
    works without network access and is ideal for air-gapped environments.
    Requires a v2 rules file with ecosystem field populated (generated by the
    weekly OSV rule update workflow). Use pkg_scan as a lightweight alternative
    to the composite scan action when only language package vulnerabilities are
    of interest.
  tags: [security, vulnerability, language-packages, offline, npm, python, rust, ruby]

spec:
  type: question
  platforms: [windows, linux, darwin]

  execution:
    plugin: vuln_scan
    action: pkg_scan
    concurrency: per-device
    timeoutSeconds: 60

  parameters:
    type: object
    properties: {}

  result:
    columns:
      - name: severity
        type: string
      - name: category
        type: string
      - name: title
        type: string
      - name: detail
        type: string
    aggregation:
      groupBy: [severity]
      operations: [count]

  readablePayload: "Scan language packages for CVEs (offline)"

  gather:
    ttlSeconds: 600

  approval:
    mode: auto

  permissions:
    executeRoles: [endpoint-admin, endpoint-operator, security-admin]
    authorRoles: [content-author]

  compatibility:
    minAgentVersion: 1.0.0
    requiredPlugins: [vuln_scan]

status:
  phase: active
